---
- name: Download WordPress
  get_url: url=http://ja.wordpress.org/wordpress-{{ wp_version }}-ja.tar.gz dest=/var/www/wordpress-{{ wp_version }}-ja.tar.gz

- name: Extract archive
  command: chdir=/var/www/ /bin/tar xvf wordpress-{{ wp_version }}-ja.tar.gz creates=/var/www/wordpress


- name: Fetch random salts for WordPress config
  local_action: command curl https://api.wordpress.org/secret-key/1.1/salt/
  register: "wp_salt"
  sudo: no

- name: Create WordPress database
  mysql_db: name={{ wp_db_name }} state=present

- name: Create WordPress database user
  mysql_user: name={{ wp_db_user }} password={{ wp_db_password }} priv={{ wp_db_name }}.*:ALL host='localhost' state=present

- name: Copy WordPress config file
  template: src=wp-config.php dest=/var/www/wordpress/

- name: Change ownership of WordPress installation
  file: path=/var/www/wordpress/ owner=nginx group=nginx state=directory recurse=yes
